# Rime console settings
# vim: set sw=2 sts=2 et:
# encoding: utf-8

schema:
  schema_id: rime_console
  name: Rime 控制台

engine:
  processors:
    - speller
    - locator
    - selector
    - express_editor
  segmentors:
    - abc_segmentor
    - fallback_segmentor
  translators:
    - echo_translator
    - r10n_translator
    - trivial_translator

translator:
  dictionary: luna_pinyin

menu:
  page_size: 10
