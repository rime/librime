# Rime schema settings
# vim: set sw=2 sts=2 et:
# encoding: utf-8

schema:
  schema_id: cangjie5
  name: 倉頡五

engine:
  processors:
    - speller
    - punctuator
    - navigator
    - selector
    - express_editor
  segmentors:
    - abc_segmentor
    - punct_segmentor
    - fallback_segmentor
  translators:
    - echo_translator
    - punct_translator
    - table_translator
    - trivial_translator

translator:
  dictionary: cangjie5

punctuator:
  import_preset: default
