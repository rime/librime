language: cpp

matrix:
  include:
    - os: osx
      compiler: clang
    - os: linux
      compiler: gcc
      dist: trusty
      sudo: required

install:
  ./travis-install.sh

script:
  ./travis-script.sh
